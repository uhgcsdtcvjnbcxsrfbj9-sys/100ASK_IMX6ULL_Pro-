I2C = Inter-Integrated Circuit
芯片间通信总线
它就是一个双线制的通信协议，用来让一个主设备和多个从设备之间互相聊天。
二、I2C 的核心特点（记住这 5 点）
双线制：只有两根线（SDA 数据线 + SCL 时钟线）。
主从架构：一个主机（如 MCU），多个从机（传感器、存储芯片）。
半双工：不能同时收发，同一时刻只能走一个方向。
多主竞争：理论支持多主，但在嵌入式中通常只用单主。
低速：标准模式 100kbps，快速模式 400kbps，高速 3.4Mbps。

I2C 硬件层长什么样？
1. 物理接线
SDA (Serial Data)：双向数据传输线。
SCL (Serial Clock)：时钟信号线，由主设备控制。
VCC / GND：电源和地。
2. 上拉电阻（必须有）
I2C 是 开漏输出 (Open Drain)。
这意味着：
器件只能把电平拉低（0）。
电平拉高（1）必须靠外部的上拉电阻。
常见配置：4.7kΩ 或 10kΩ 电阻接 VCC。



I2C 通信流程（从初始化到结束）
1. 起始信号 (Start Condition)
动作：当 SCL 保持高电平时，SDA 由高变低。
含义：通知所有从设备：“我要开始说话了，请安静听”。
底层原理：I2C 总线空闲时 SDA/SCL 均为高电平，start 信号是唯一能打破空闲状态的信号，确保总线同步
代码：SCL=1, SDA=1 → SCL=1, SDA=0。

2. 数据传输 (Data Transmission)
每次传输 8 位（一个字节）。
先发高位 (MSB)，后发低位 (LSB)。
每传完 8 位，需要接收方发一个应答 (ACK)。
3. 应答机制 (ACK / NACK)
ACK (0)：接收方收到了数据，准备好下一个。
NACK (1)：接收方没收到，或拒绝继续接收。
4. 停止信号 (Stop Condition)
动作：当 SCL 保持高电平时，SDA 由低变高。
含义：通信结束，释放总线。
代码：SCL=1, SDA=0 → SCL=1, SDA=1。

六、设备地址（I2C 灵魂）
I2C 支持多个设备挂在同一根线上，靠地址区分。
7 位地址：支持 127 个设备。
10 位地址：支持 1023 个设备（较少用）。
发送流程：
主机发送 7 位地址。
发送一位读写控制位（0 = 写，1 = 读）。
从机应答（ACK）。
开始数据传输。





我用最直白、最实用、不绕弯的方式告诉你：
学会 I2C 协议，最主要就学会 4 件事
这 4 件事是整个 I2C 的核心，学会这 4 点，你就完全掌握了 I2C。
1. 看懂总线时序：start、数据、ack、stop
这是 I2C 的底层基本功。
你必须彻底理解：
起始信号 (Start)：SCL 高时 SDA 从高变低
数据有效：SCL 高电平期间，SDA 必须稳定
应答 (ACK)：每传 1 字节必须回 1 个 ACK
停止信号 (Stop)：SCL 高时 SDA 从低变高
为什么要学？
你看波形、调示波器、查芯片手册都靠它
写裸机 I2C 代码靠它
理解驱动底层靠它


. 学会处理多设备：总线冲突、应答、寻址规则
I2C 是总线结构，一个 SDA/SCL 挂多个设备。
你要学会：
7 位地址唯一区分设备
ACK 验证设备是否在线
多主总线仲裁（嵌入式一般不用，但要知道概念）
上拉电阻的作用（开漏输出必须靠电阻拉高）










