UART = Universal Asynchronous Receiver/Transmitter，通用异步收发传输器
它是一种串行、异步、全双工的通信协议 / 硬件模块，核心作用是：
把并行数据（CPU 内部 8/16/32 位总线） → 转换成串行比特流（单根线逐位发送）
把串行比特流 → 还原成并行数据（给 CPU 处理）
核心特性（必须记住）
串行通信	数据按位依次传输，仅需 2 根线（TX/RX）即可双向通信
异步通信	收发双方不需要共享时钟，靠「波特率 + 起始 / 停止位」同步
全双工	TX 和 RX 独立工作，可同时收发数据
点对点	标准 UART 仅支持 1 对 1 通信（多设备需 RS485 等扩展）
低速率	常见波特率 9600~3Mbps，适合短距离、低带宽场景

  标准 UART 引脚定义
最基础的 UART 仅需 2 根核心信号线，完整 9 针 RS232 接口包含更多流控引脚：
引脚名	全称	方向	核心功能
TX	Transmitter	输出	发送数据（发送端 → 接收端）
RX	Receiver	输入	接收数据（接收端 ← 发送端）
GND	Ground	公共	参考地（必须共地，否则电平无法识别）
RTS	Request To Send	输出	硬件流控：请求发送（本方准备好接收）
CTS	Clear To Send	输入	硬件流控：允许发送（对方准备好接收）
DTR	Data Terminal Ready	输出	数据终端就绪（本方设备就绪）
DSR	Data Set Ready	输入	数据装置就绪（对方设备就绪）
DCD	Data Carrier Detect	输入	载波检测（检测连接状态）
RI	Ring Indicator	输入	振铃指示（拨号上网时代遗留）
💡 嵌入式开发中，99% 场景仅用 TX/RX/GND 三根线，流控引脚仅在高速 / 大数据量时使用。


   电气电平标准（3 种核心类型）
UART 是逻辑层协议，物理层可以适配不同电平标准，这是硬件设计的核心：
（1）TTL 电平（最常用，嵌入式 MCU 原生）
逻辑 1：高电平（3.3V 或 5V，由 MCU 供电决定）
逻辑 0：低电平（0V）
特点：直接由 MCU GPIO 输出，无需转换芯片，传输距离短（<1m），抗干扰弱
适用场景：开发板内部、MCU 与传感器 / 蓝牙模块短距离通信
（2）RS232 电平（传统串口，电脑原生）
逻辑 1：负电平（-3V ~ -15V，典型 -12V）
逻辑 0：正电平（+3V ~ +15V，典型 +12V）
特点：抗干扰能力强于 TTL，传输距离可达 15m，需电平转换芯片（MAX232/SP3232）
适用场景：电脑与工业设备、老款仪器通信
（3）RS485 电平（差分，工业级）
逻辑 1：A 线 > B 线（+2V ~ +6V）
逻辑 0：A 线 < B 线（-2V ~ -6V）
特点：差分传输抗干扰极强，传输距离可达 1200m，支持多节点（1 对 N），半双工
适用场景：工业现场、长距离、多设备组网
⚠️ 关键注意：TTL、RS232、RS485 电平不兼容，直接连接会烧毁芯片！ 必须用转换芯片（如 MAX232、MAX485）。

异步通信的本质是：收发双方没有共享时钟，靠「帧格式 + 波特率」实现同步。
收发双方波特率必须完全一致，否则会出现乱码、数据错误


  UART 不是简单的「线转线」，而是一个独立的硬件模块，内部包含完整的收发控制逻辑，以 MCU 内部 UART 为例：
1. UART 发送端（TX）结构与流程
核心硬件模块
发送数据寄存器（TDR）：CPU 写入待发送的并行数据（8 位）
发送移位寄存器（TSR）：把并行数据转换成串行比特流
波特率发生器（Baud Rate Generator）：根据配置的波特率，生成发送时钟
发送控制逻辑：自动添加起始位、校验位、停止位，控制移位输出
发送流程（硬件自动完成，CPU 仅需写寄存器）
CPU 把待发送的 8 位数据写入 TDR 寄存器
硬件自动把 TDR 数据加载到 TSR 移位寄存器，并置位「发送完成标志」
波特率发生器生成发送时钟，TSR 按位时钟逐位输出：
先输出 1 位起始位（低电平）
再输出 8 位数据位（LSB first）
（可选）输出 1 位校验位
最后输出 1~2 位停止位（高电平）
一帧发送完成后，TSR 自动加载下一个 TDR 中的数据（若有），循环发送
发送完成后，硬件触发发送中断（可选），通知 CPU 写入下一个数据
2. UART 接收端（RX）结构与流程
核心硬件模块
接收数据寄存器（RDR）：存储还原后的并行数据，供 CPU 读取
接收移位寄存器（RSR）：把串行比特流转换成并行数据
波特率发生器（Baud Rate Generator）：生成接收采样时钟（通常为 16 倍波特率，用于过采样）
接收控制逻辑：检测起始位、采样数据、校验、检测帧错误
错误检测模块：检测帧错误、溢出错误、奇偶错误、噪声错误
接收流程（硬件自动完成，CPU 仅需读寄存器）
空闲时，RX 线为高电平，硬件持续检测「高→低」的起始位跳变
检测到起始位后，硬件以 16 倍波特率 采样，在第 8 个采样点（中间位置）确认起始位有效，避免噪声误触发
按位时钟（波特率）逐位采样 RX 线：
采样 8 位数据位，存入 RSR 移位寄存器
（可选）采样校验位，进行奇偶校验
采样停止位，确认帧结束
一帧数据接收完成后，RSR 把并行数据加载到 RDR 寄存器
硬件置位「接收完成标志」，触发接收中断（可选），通知 CPU 读取数据
CPU 读取 RDR 寄存器，完成数据接收



  UART 硬件设计关键要点（嵌入式实战）
1. 接线规范
TX ↔ RX，RX ↔ TX：收发端必须交叉连接（A 的 TX 接 B 的 RX，A 的 RX 接 B 的 TX）
必须共地：GND 必须连接，否则电平参考不一致，会出现乱码
避免长距离：TTL 电平传输距离 <1m，RS232 <15m，RS485 <1200m
抗干扰设计：用屏蔽线、增加 TVS 管、终端电阻（RS485 必须 120Ω 终端电阻）

  
  电平转换芯片 = 电压翻译官
两个芯片 / 设备的高电平、低电平标准不一样，不能直接连，
必须用一颗芯片把电压 “翻译” 成对双方都安全、都能识别的电压，这颗芯片就叫电平转换芯片。
用最通俗的例子
现在最常见的场景：
单片机（MCU）：3.3V 电平
高电平 = 3.3V
低电平 = 0V
传感器 / 旧模块：5V 电平
高电平 = 5V
低电平 = 0V
如果你直接把 5V 模块的输出接到 3.3V 单片机：
👉 5V 电压会直接灌进 3.3V 芯片
👉 直接烧坏单片机引脚
电平转换芯片就是干这个的：
把 5V → 安全的 3.3V
把 3.3V → 标准 5V
两边都能识别，又不会烧硬件。
电平转换芯片到底是什么？
官方定义：
实现不同电压域之间数字信号相互转换的集成电路，
主要解决：
电压不兼容
防止高压损坏低压芯片
保证信号能被双方正确识别
常见名字：
电平转换芯片
电压电平转换器
level shifter
电压 translator
为什么必须用它？（核心原因）
1. 耐压问题（最关键）
大多数现代 MCU、ARM 都是 3.3V 器件
引脚耐压一般只有 3.6V 左右
你直接输入 5V，超过耐压 → 瞬间击穿、烧芯片
2. 识别阈值问题
即使没烧坏，也可能识别错误：
3.3V 输出给 5V 系统时，3.3V 可能达不到 5V 系统的 “高电平门槛”
导致对方认为一直是低电平 → 通信失败、乱码
3. 双向通信需要匹配
像 UART、I2C、SPI 都是双向通信
必须同时支持：
A → B 方向转换
B → A 方向转换
电平转换芯片内部已经做好了方向控制，不用你自己搭电路。



  
  
  
