我把这两张图里的「驱动进化之路」拆解得明明白白，从传统写法到总线模型，再到设备树，


一、先搞懂核心矛盾：驱动开发的永恒痛点
驱动开发里，永远绕不开这两个东西：
驱动逻辑：怎么操作硬件寄存器、怎么实现open/read/write/ioctl这些接口
硬件资源：用哪个 GPIO 引脚、寄存器地址、中断号这些和具体板子相关的信息
传统写法里，这俩是写死在一起的，换个引脚、换个板子就要大改代码，扩展性极差。
驱动的三次进化，本质上就是不断把「驱动逻辑」和「硬件资源」拆开，解耦得越来越彻底。

二、阶段 1：传统写法 —— 简单粗暴，但毫无扩展性
架构图（图 9.2）
plaintext
app层：open/read/write/ioctl
  ↓
driver层：drv_open/drv_read/drv_write/drv_ioctl（和硬件操作写死在一起）
  ↓
硬件层：硬件初始化、读/写寄存器（引脚、地址全写死）
代码特点
用哪个引脚、怎么操作引脚、寄存器地址，全写死在同一个驱动文件里
比如 LED 驱动里，直接写死GPIO1_3的寄存器地址，点亮 LED 时直接操作这个地址
优缺点
✅ 优点：最简单，不用任何框架，几行代码就能跑，适合快速验证功能
❌ 缺点：
毫无扩展性：换个引脚、换个板子，必须修改驱动代码，重新编译
复用性差：换个板子，驱动几乎要重写一遍
维护困难：硬件变了，驱动逻辑也要跟着改，耦合度极高



总线设备驱动模型
以前老式驱动：
引脚、寄存器地址、硬件操作代码 全部写死在一个文件
👉 换块开发板、换个引脚，驱动代码全要改，烂维护、不能复用。
所以 Linux 设计出一套规则：
硬性拆分（核心灵魂）
设备 device：只管写【硬件资源】
寄存器地址、中断号、引脚
没有任何操作代码
驱动 driver：只管写【控制逻辑】
怎么初始化、怎么读写寄存器、点灯、收发数据
不写任何硬件地址、引脚
总线 bus：中间人，负责：
保存所有设备、所有驱动
负责：设备 和 驱动 互相匹配、配对
二、三个角色分别是什么
1. Bus 总线（platform 虚拟总线）
不是硬件总线，是内核虚拟出来的一条总线
所有板载设备（LED、按键、串口、控制器）都挂在这条总线上
作用：
维护两条链表
设备链表：放所有 platform_device
驱动链表：放所有 platform_driver
自动对比、自动匹配、匹配成功自动调用 probe
2. Device 设备（platform_device）
只干一件事：描述板子上的硬件信息
内容：
设备名字（用来匹配）
物理寄存器地址
中断号
硬件起始 / 结束地址
👉 纯数据，没有函数、没有操作逻辑。
3. Driver 驱动（platform_driver）
只干一件事：写硬件的控制代码
内容：
驱动名字
probe 函数（配对成功自动执行）
remove 函数（卸载时执行）
各种硬件初始化、寄存器操作
👉 纯逻辑，不知道具体地址，地址从设备拿。

三、最重要：三者完整工作流程（背下来）
步骤 1：注册 设备 device
把写好的硬件资源结构体，挂载到 platform 总线的「设备链表」
步骤 2：注册 驱动 driver
把写好的驱动代码，挂载到 platform 总线的「驱动链表」
步骤 3：总线开始【匹配】
谁后注册，谁就遍历对方链表：
先注册设备 → 后注册驱动：驱动遍历所有设备
先注册驱动 → 后注册设备：设备遍历所有驱动
步骤 4：三条匹配规则（按顺序，从上到下）
优先：driver_override 强制指定驱动名
其次：驱动里的 id_table 设备匹配表（一对多）
最后：直接对比 设备.name 和 驱动.name
名字对上 = 匹配成功
步骤 5：匹配成功 → 自动执行驱动的 probe 函数
probe 是一切的入口：
在 probe 里，驱动主动 从设备中读取硬件资源
ioremap 把物理地址转为内核虚拟地址
初始化硬件、注册字符设备、实现读写接口
应用层就能正常使用硬件
步骤 6：卸载模块时
自动调用 remove 函数，释放资源、注销设备。


