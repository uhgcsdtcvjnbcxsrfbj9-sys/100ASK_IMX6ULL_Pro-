API 操作 = 应用程序调用系统函数，对设备进行读写与控制的行为。
  在输入子系统里，就是：
应用怎么读 event 事件、怎么等事件、怎么查询设备信息
  API 操作 = 应用程序怎么用这个设备
  我们之前说过，APP 通过 /dev/input/eventX 这个字符设备节点读取输入事件。而 Linux 为这个设备节点完整实现了标准的文件 I/O 操作机制，让你可以用不同的方式处理输入事件，适配不同的业务场景。


  这 4 种机制分别是：阻塞、非阻塞、POLL/SELECT、异步通知，本质都是「APP 如何等待 / 获取输入事件」的不同方


  1. 阻塞式 I/O（最基础、最常用）
核心原理
默认行为：当你用 open() 打开 /dev/input/eventX 后，调用 read() 读取事件时，如果没有新的输入事件，read() 会一直卡住（阻塞），直到有事件到来才返回。
特点：简单、省 CPU，APP 在等待时会进入休眠，不占用系统资源。
适用场景：只需要处理输入事件，不需要同时做其他任务的程序（比如简单的按键测试程序）。

   非阻塞式 I/O
核心原理
主动轮询：打开设备时加上 O_NONBLOCK 标志，read() 就不会阻塞了。如果没有事件，read() 会立即返回错误（-1），并设置 errno = EAGAIN。
特点：APP 不会卡住，可以继续做其他任务，但需要循环轮询，会占用 CPU 资源。
适用场景：需要同时处理多个任务（比如一边读按键，一边刷新屏幕），但不想用多线程的场景。

   POLL/SELECT 多路复用
核心原理
同时监听多个文件描述符：select()/poll() 可以让 APP同时监听多个设备节点（比如同时监听按键、触摸屏、串口），当任意一个设备有事件到来时，才唤醒 APP 处理。
特点：既不会像阻塞式那样只能等一个设备，也不会像非阻塞式那样浪费 CPU，是嵌入式开发中最常用的多设备处理方案。
适用场景：需要同时处理多个输入设备（比如按键 + 触摸屏 + 鼠标），或者同时处理输入和网络、串口等 I/O 的场景。


   异步通知（信号驱动 I/O）
核心原理
事件主动通知：APP 向内核注册一个信号处理函数，当有输入事件到来时，内核会主动给 APP 发送一个信号（比如 SIGIO），APP 收到信号后再去读取事件。
特点：APP 完全不需要轮询，也不需要阻塞等待，只有事件到来时才被唤醒，CPU 利用率最高，适合后台服务、守护进程。
适用场景：后台运行的输入事件处理程序（比如按键唤醒系统、触摸屏手势识别），不需要一直占用 CPU。


  守护进程 = 后台一直运行、不退出的程序
一句话定义
守护进程（daemon）
就是：在后台默默运行、不占终端、开机自启、一直不退出的系统服务程序。
生活类比（秒懂）
你打开一个终端，运行 ./test
→ 这是前台程序，关掉终端它就死了。
守护进程就像：
→ 手机后台的闹钟服务
→ 电脑的蓝牙服务
→ 路由器的 WiFi 服务
你看不见它窗口，但它一直在运行，随时待命。
守护进程的 4 个特点
没有终端，不依赖窗口
关掉 SSH、关掉串口终端，它照样跑。
一直在后台循环运行
不会自己退出，除非被杀掉。
通常开机自动启动
系统一跑起来它就跟着启动。
默默干活，不打扰用户
比如：
按键监听服务
网络后台服务
系统状态监测

  
