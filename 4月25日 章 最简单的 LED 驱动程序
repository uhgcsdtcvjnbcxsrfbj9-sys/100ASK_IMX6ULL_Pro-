字符设备驱动程序框架 
一、先搞懂：字符设备驱动是干嘛的？
Linux 里，所有硬件设备都被抽象成 “文件”，比如：
LED、按键、串口、I2C 都叫「字符设备」（按字节流读写）
APP 要操作硬件，就像操作普通文件一样：open() 打开、read() 读数据、write() 写数据、ioctl() 控制。
驱动的作用，就是把 APP 的这些文件操作，翻译成对硬件寄存器的操作。

二、流程图拆解：APP → 内核 → 驱动 → 硬件
你给的图，是 Linux 字符设备的完整调用链，从上到下一层一层讲：
1. 最上层：APP（用户态）
APP 用 C 库函数调用：open()、read()、write()、ioctl()
这是用户态，APP 不能直接操作硬件（权限不够）
比如 APP 想点亮 LED，会调用 write(fd, &cmd, 1)，但它不知道怎么控制寄存器，得交给内核处理。
2. 中间层：内核态（系统调用）
APP 的 open/read/write/ioctl 最终都会触发 svc 指令（ARM 架构下的软中断指令），陷入内核，触发系统调用异常处理。
这一步叫「用户态→内核态切换」
内核会根据 APP 打开的文件描述符，找到对应的设备驱动，把请求转发给驱动里的对应函数。
3. 驱动层：我们写的代码
驱动里要实现和 APP 操作对应的函数：
drv_open：APP 调用 open() 时，内核会执行这个函数（比如初始化硬件、设置引脚方向）
drv_read：APP 调用 read() 时，内核会执行这个函数（比如读取按键电平，返回给 APP）
drv_write：APP 调用 write() 时，内核会执行这个函数（比如控制 LED 亮灭）
drv_ioctl：APP 调用 ioctl() 时，内核会执行这个函数（比如设置 LED 亮度、模式）
关键函数：
ioremap：把硬件寄存器的物理地址，映射成内核能直接访问的虚拟地址，驱动才能读写寄存器控制硬件
copy_to_user：把驱动里的数据（比如读到的按键值），拷贝给 APP 的用户态空间
copy_from_user：把 APP 发来的数据（比如亮灯指令），拷贝到驱动的内核态空间
4. 最底层：硬件
驱动通过读写虚拟地址（映射后的寄存器），直接控制硬件引脚、外设，比如拉高 GPIO 电平点亮 LED。


编写驱动程序的套路：
确定主设备号，也可以让内核分配
定义自己的 file_operations 结构体
实现对应的 drv_open/drv_read/drv_write 等函数，填入 file_operations 结构
体
把 file_operations 结构体告诉内核：register_chrdev
谁来注册驱动程序啊？得有一个入口函数：安装驱动程序时，就会去调用这
个入口函数
有入口函数就应该有出口函数：卸载驱动程序时，出口函数调用
unregister_chrdev
其他完善：提供设备信息，自动创建设备节点：class_create,device_create
⚫ 驱动怎么操作硬件？
◼ 通过 ioremap 映射寄存器的物理地址得到虚拟地址，读写虚拟地址。
驱动怎么操作硬件？——ioremap
硬件寄存器只有物理地址（比如 0x20E00000），内核态不能直接访问物理地址，所以：
用 ioremap(物理地址, 大小)，把物理地址映射成虚拟地址
驱动直接读写这个虚拟地址，就等于操作硬件寄存器了
2. 驱动怎么和 APP 传输数据？——copy_to_user / copy_from_user
APP 运行在用户态，驱动运行在内核态，两个地址空间是隔离的，不能直接传指针，所以要用这两个函数拷贝数据：
copy_from_user(内核地址, 用户地址, 大小)：把 APP 发来的数据，拷贝到驱动里
copy_to_user(用户地址, 内核地址, 大小)：把驱动里的数据，拷贝给 APP


5.2.2 实现什么功能
先编写驱动程序：
⚫ 实现 led_open 函数，在里面初始化 LED 引脚。
⚫ 实现 led_write 函数，在里面根据 APP 传来的值控制 LED。
⚫ 再编写测试程序。


