poll/select 机制：带闹钟的「多任务等数据」方案
这一节讲的是 Linux 里I/O 多路复用的核心机制，本质就是你之前学的「妈妈定闹钟陪孩子睡」的技术实现。我会从原理、流程、代码、对比四个维度，给你讲得明明白白。

  poll/select 完美解决了这两个痛点：
✅ 既不会一直占 CPU（没数据就休眠）
✅ 又能同时监听多个设备 / 文件
✅ 还能设置超时，到点自动醒来干别的活

  poll 和 select 本质完全一样，只是 API 接口不同。它们都是「带超时的阻塞等待」：APP 把要监听的文件（设备）交给内核，内核帮它盯着，有数据 / 超时就唤醒 APP。

   APP 侧的操作步骤
open 设备：打开要监听的硬件设备（比如 /dev/input/event0 按键），拿到文件描述符 fd
配置监听列表：告诉 poll/select：
要监听哪些 fd（文件 / 设备）
每个 fd 要监听什么事件（比如 POLLIN = 有数据可读）
超时时间（比如 5000ms = 5 秒）
调用 poll/select：APP 进入阻塞休眠，把监听任务交给内核
被唤醒后判断：根据返回值，知道哪个 fd 有事件，然后调用 read 读数据
2. 内核 + 驱动侧的操作
内核收到 poll/select 请求后，会遍历所有要监听的 fd，调用对应驱动的 poll 函数
驱动的 poll 函数会：
检查自己的缓冲区有没有数据
把 APP 挂到驱动的等待队列上（wait_queue_head_t）
内核开始等待：
硬件产生数据 → 驱动中断函数唤醒等待队列 → 内核唤醒 APP → poll/select 立刻返回
超时时间到了 → 内核自动唤醒 APP → poll/select 返回 0（超时）
APP 被唤醒后，检查哪个 fd 有事件，调用 read 读数据（此时驱动缓冲区一定有数据，read 立刻返回）
  APP 被唤醒后，检查哪个 fd 有事件，调用 read 读数据（此时驱动缓冲区一定有数据，read 立刻返回）

poll 函数详解（图中代码逐句拆解）
1. 函数原型
int poll(struct pollfd *fds, nfds_t nfds, int timeout);
表格
参数	含义
fds	监听列表数组，每个元素是一个 struct pollfd，描述一个要监听的 fd
nfds	监听列表的长度（要监听多少个 fd）
timeout	超时时间，单位毫秒：
- -1：无限等待，直到有事件
- 0：立刻返回，不阻塞
- >0：等待 timeout 毫秒
返回值	>0：有事件发生，返回有事件的 fd 数量
0：超时
-1：出错

  
  poll/select 可监听的事件类型（表 7-1 详解）
表格
事件类型	说明	常用场景
POLLIN	有数据可读	最常用！监听按键、串口、传感器等输入设备
POLLRDNORM	等同于 POLLIN	兼容旧系统，一般直接用 POLLIN
POLLRDBAND	有优先级带数据可读	Linux 几乎不用
POLLPRI	高优先级数据可读	特殊硬件（比如带优先级的串口）
POLLOUT	可以写数据	监听串口、网卡等输出设备，缓冲区有空位可写
POLLWRNORM	等同于 POLLOUT	兼容旧系统，一般直接用 POLLOUT
POLLWRBAND	优先级数据可写	几乎不用
POLLERR	发生了错误	异常判断，比如设备断开
POLLHUP	挂起（设备断开连接）	异常判断，比如串口拔了
POLLNVAL	无效请求（fd 未打开）	错误排查

  APP 先调用 open 函数时。
⚫ APP 不是直接调用 read 函数，而是先调用 poll 或 select 函数，这 2 个函
数中可以传入“超时时间”。它们的作用是：如果驱动程序中有数据，则立刻返回；
否则就休眠。在休眠期间，如果有人操作了硬件，驱动程序获得数据后就会把 APP
唤醒，导致 poll 或 select 立刻返回；如果在“超时时间”内无人操作硬件，则
时间到后 poll 或 select 函数也会返回。APP 可以根据函数的返回值判断返回
原因：有数据？无数据超时返回？
⚫ APP 根据 poll 或 select 的返回值判断有数据之后，就调用 read 函数读取
数据时，这时就会立刻获得数据。
⚫ poll/select 函数可以监测多个文件，可以监测多种事件：


  
