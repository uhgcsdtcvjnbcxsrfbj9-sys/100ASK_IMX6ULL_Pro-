.1 核心概念：文件句柄与struct file
当用户空间的 APP 调用open()打开一个文件时，内核会做两件核心事：
1,返回一个整数文件句柄（文件描述符 fd）：这个 fd 是用户空间用来索引文件的唯一标识，本质是进程的files_struct结构体中fd_array数组的下标。
2,在内核中创建一个struct file结构体实例：每一个 APP 的打开操作，都会对应一个独立的struct file，哪怕是同一个文件被打开多次，也会生成多个独立的struct file（如图 1.2 所示）。




struct file {
    union {
        struct llist_node fu_llist;
        struct rcu_head fu_rcuhead;
    } f_u;
    struct path f_path;          // 文件路径（挂载点+目录项）
    struct inode *f_inode;       // 核心：指向文件的inode（文件的元数据，磁盘持久化）
    const struct file_operations *f_op;  // 核心：文件操作函数表（驱动的核心入口）
    spinlock_t f_lock;
    atomic_long_t f_count;       // 引用计数，用于判断文件是否被关闭
    unsigned int f_flags;        // open()传入的flags（O_RDONLY/O_WRONLY/O_RDWR等）
    fmode_t f_mode;              // open()传入的mode（文件权限，如0644）
    struct mutex f_pos_lock;
    loff_t f_pos;                // 核心：文件当前读写偏移量（lseek修改的就是它）
    struct fown_struct f_owner;  // 异步通知相关（信号发送的进程/组）
    const struct cred *f_cred;   // 文件的凭据（UID/GID，权限校验）
    struct file_ra_state f_ra;   // 预读状态
    // ... 其他字段省略
};


f_op	文件操作函数表指针	这是用户态read/write/ioctl等系统调用的内核分发入口。当 APP 调用read(fd, ...)时，内核会通过 fd 找到struct file，再通过f_op调用对应的read函数。对于普通文件，f_op指向 VFS 的默认实现；对于设备文件，f_op指向我们自己编写的驱动函数表。
f_flags	存储open()的flags参数	比如O_RDONLY(0)、O_WRONLY(1)、O_RDWR(2)、O_CREAT(0x40)、O_APPEND(0x400) 等，驱动中可以通过file->f_flags判断文件的打开方式。
f_mode	存储open()的mode参数	即文件权限（如S_IRUSR/S_IWUSR等），用于权限校验，open()的mode会被进程的umask掩码修正后写入 inode。
f_pos	文件当前读写偏移量	每次read/write都会自动更新这个值，lseek()系统调用的本质就是修改这个字段。注意：struct file是进程私有，所以同一个文件被多个进程打开时，各自的f_pos独立互不影响。
f_inode	指向struct inode	inode是文件的元数据本体，存储在磁盘上（普通文件），包含文件大小、权限、磁盘块位置、设备号（设备文件）等。多个struct file可以指向同一个inode（比如dup()复制文件描述符、父子进程继承 fd），但inode只有一份。
1.3 核心逻辑：open()系统调用的完整流程
当 APP 执行int fd = open(const char *pathname, int flags, mode_t mode);时，内核的执行流程是：
路径解析：VFS 根据pathname找到文件的dentry（目录项），进而找到对应的inode。
分配struct file：内核从filp缓存中分配一个新的struct file结构体，初始化f_flags/f_mode/f_pos（初始为 0）等字段。
绑定操作函数表：根据inode的类型（普通文件 / 目录 / 设备文件），给f_op赋值：
普通文件：f_op = inode->i_fop（如ext2_file_operations）
字符设备文件：f_op = 驱动注册的file_operations` 结构体
分配文件描述符 fd：在进程的files_struct中找到最小的空闲下标，将struct file的指针存入fd_array[fd]，返回 fd 给用户空间。
调用驱动的open函数：如果是设备文件，内核会调用f_op->open()，完成驱动的初始化（如硬件初始化、数据结构分配）。



字符设备驱动的编写流程
 步骤①：确定主设备号
做什么
主设备号（Major Number）是 Linux 内核用来区分不同类型设备驱动的唯一标识，次设备号（Minor Number）用来区分同类型的多个设备。
静态指定：比如#define MY_MAJOR 240（1-255 之间，避免与系统占用的主设备号冲突）
动态分配：让内核自动分配一个空闲的主设备号（推荐，避免冲突）
为什么要做
Linux 内核通过主设备号将字符设备文件与对应的驱动程序绑定：
当open()一个字符设备文件（如/dev/mydev）时，内核会从inode中取出dev_t（包含主 / 次设备号），然后根据主设备号在chrdevs数组（内核的字符设备注册表）中找到对应的驱动。
没有主设备号，内核无法将用户的文件操作分发到对应的驱动。


 步骤②：定义自己的file_operations结构体
做什么
struct file_operations是内核定义的函数指针结构体，是用户态系统调用与驱动函数的桥梁。我们需要定义一个自己的实例，填充需要实现的函数指针。
// 内核定义的结构体（简化版）
struct file_operations {
    struct module *owner;        // 必须填THIS_MODULE，防止模块被卸载
    loff_t (*llseek) (struct file *, loff_t, int);  // lseek系统调用对应
    ssize_t (*read) (struct file *, char __user *, size_t, loff_t *);  // read系统调用对应
    ssize_t (*write) (struct file *, const char __user *, size_t, loff_t *);  // write系统调用对应
    long (*unlocked_ioctl) (struct file *, unsigned int, unsigned long);  // ioctl对应
    int (*open) (struct inode *, struct file *);  // open系统调用对应
    int (*release) (struct inode *, struct file *);  // close系统调用对应
    // ... 其他函数指针
};
为什么要做
这是驱动的核心入口：
当用户态调用read(fd, buf, size)时，内核会通过struct file->f_op找到我们定义的read函数，然后调用它。
没有这个结构体，内核就不知道该调用哪个驱动函数来处理用户的文件操作。


步骤③：实现对应的open/read/write等函数，填入结构体
做什么
我们需要实现file_operations中声明的函数，比如my_open/my_read/my_write/my_release，然后将函数指针赋值给结构体。
// 示例：实现open函数
static int mydev_open(struct inode *inode, struct file *filp)
{
    printk(KERN_INFO "mydev open called\n");
    return 0; // 0表示成功，负数表示错误
}

// 示例：实现read函数
static ssize_t mydev_read(struct file *filp, char __user *buf, size_t size, loff_t *ppos)
{
    // 从内核空间拷贝数据到用户空间（必须用copy_to_user，防止用户空间地址非法）
    if (copy_to_user(buf, kernel_buf, size)) {
        return -EFAULT;
    }
    return size; // 返回实际读取的字节数
}

// 定义自己的file_operations结构体
static const struct file_operations mydev_fops = {
    .owner = THIS_MODULE,
    .open = mydev_open,
    .read = mydev_read,
    .write = mydev_write,
    .release = mydev_release,
};
为什么要做
这些函数是驱动的业务逻辑实现：
open：设备打开时的初始化（如硬件上电、分配缓冲区）
read：用户从设备读取数据（如从传感器读数据、从驱动缓冲区读数据）
write：用户向设备写入数据（如向硬件发命令、向驱动缓冲区写数据）
release：设备关闭时的清理（如释放缓冲区、硬件断电）
必须用copy_to_user/copy_from_user：用户空间地址是虚拟地址，内核不能直接访问，必须通过内核提供的安全函数拷贝，防止非法地址访问导致内核崩溃。



 步骤④：把file_operations结构体告诉内核：注册驱动程序
做什么
使用内核提供的注册函数，将我们的驱动（主设备号 +file_operations）注册到内核的字符设备注册表中。
老版本（2.6 以前）：register_chrdev(unsigned int major, const char *name, const struct file_operations *fops)
新版本（推荐）：cdev_init + cdev_add（更安全，支持动态主设备号）
// 定义字符设备结构体
static struct cdev my_cdev;

// 注册驱动
static int __init mydev_init(void)
{
    dev_t devno = MKDEV(MY_MAJOR, 0); // 构造设备号（主+次）
    cdev_init(&my_cdev, &mydev_fops); // 初始化cdev，绑定fops
    cdev_add(&my_cdev, devno, 1);     // 向内核添加字符设备
    printk(KERN_INFO "mydev driver registered\n");
    return 0;
}
为什么要做
内核需要一个注册表来管理所有字符设备驱动：
cdev_add会将我们的struct cdev（包含file_operations）添加到内核的chrdevs数组中，以主设备号为索引。
当用户open设备文件时，内核通过主设备号在chrdevs中找到对应的cdev，进而获取file_operations，完成系统调用的分发。
不注册的话，内核根本不知道这个驱动的存在，open设备文件会报No such device or address错误。



步骤⑤：入口函数：安装驱动时调用
做什么
驱动是 Linux 内核模块（.ko文件），必须有一个模块入口函数，用module_init宏指定，当执行insmod mydev.ko时，内核会自动调用这个函数。
// 模块入口函数
static int __init mydev_init(void)
{
    // 步骤④的注册逻辑在这里执行
    int ret = register_chrdev(MY_MAJOR, "mydev", &mydev_fops);
    if (ret < 0) {
        printk(KERN_ERR "mydev register failed\n");
        return ret;
    }
    return 0;
}
module_init(mydev_init); // 指定入口函数
为什么要做
Linux 内核模块是动态加载的，不是编译进内核的：
入口函数是驱动的启动入口，负责完成驱动的初始化（注册设备、硬件初始化、分配资源）。
没有入口函数，insmod时内核不知道该执行什么，驱动无法加载。



 步骤⑥：出口函数：卸载驱动时调用
做什么
对应入口函数，必须有一个模块出口函数，用module_exit宏指定，当执行rmmod mydev时，内核会自动调用这个函数，完成资源清理。
// 模块出口函数
static void __exit mydev_exit(void)
{
    // 注销驱动，释放资源
    unregister_chrdev(MY_MAJOR, "mydev");
    printk(KERN_INFO "mydev driver unregistered\n");
}
module_exit(mydev_exit); // 指定出口函数
为什么要做
出口函数是驱动的清理入口，负责注销设备、释放内存、关闭硬件，防止资源泄漏。
没有出口函数，rmmod时无法卸载驱动，会导致内核资源泄漏，甚至系统崩溃。
必须与入口函数配对：入口做了什么初始化，出口就要做对应的反初始化。


步骤⑦：其他完善：提供设备信息，自动创建设备节点
做什么
驱动加载后，需要在/dev目录下创建设备文件（节点），才能让用户 APP 通过open()访问设备。
手动创建：mknod /dev/mydev c 240 0（c表示字符设备，240 是主设备号，0 是次设备号）
自动创建（推荐）：使用class_create + device_create，让内核自动创建设备节点（udev 机制）
// 定义类和设备结构体
static struct class *mydev_class;
static struct device *mydev_device;

// 在入口函数中添加
static int __init mydev_init(void)
{
    // ... 注册驱动 ...
    // 创建类（在/sys/class/下生成mydev目录）
    mydev_class = class_create(THIS_MODULE, "mydev");
    // 创建设备节点（自动在/dev下生成mydev文件）
    mydev_device = device_create(mydev_class, NULL, MKDEV(MY_MAJOR, 0), NULL, "mydev");
    return 0;
}

// 在出口函数中添加
static void __exit mydev_exit(void)
{
    // ... 注销驱动 ...
    device_destroy(mydev_class, MKDEV(MY_MAJOR, 0)); // 删除设备节点
    class_destroy(mydev_class); // 删除类
}
为什么要做
用户空间的 APP 只能通过/dev下的设备文件访问驱动，没有设备节点，APP 无法open设备。
自动创建的好处：不需要手动执行mknod，驱动加载时自动生成，卸载时自动删除，更方便、更安全。



用户态 - 内核态的桥梁：struct file是用户态文件句柄的内核表示，file_operations是用户态系统调用的内核分发入口。
驱动的本质：字符设备驱动的核心就是实现file_operations中的函数，让内核能将用户的open/read/write等操作转发到我们的驱动代码中。
注册的本质：cdev_add/register_chrdev是将驱动的file_operations注册到内核的字符设备注册表中，让内核能通过主设备号找到驱动。
设备节点的本质：/dev下的设备文件是用户访问驱动的入口，inode中存储了主 / 次设备号，内核通过它找到对应的驱动。
安全机制：copy_to_user/copy_from_user是内核的安全检查，防止用户空间的非法地址访问导致内核崩溃。









