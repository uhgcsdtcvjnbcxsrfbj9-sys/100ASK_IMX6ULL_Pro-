触摸屏分为电阻屏、电容屏。电阻屏结构简单，在以前很流行；电容屏支持
多点触摸，现在的手机基本都是使用电容屏。LCD、触摸屏不是一回事，LCD 是输出设备，触摸屏是输入设备。制作触摸
屏时特意把它的尺寸做得跟 LCD 一模一样，并且把触摸屏覆盖在 LCD 上。
 电阻屏
复习一下欧姆定律
长度和阻值成正比关系

我会从底层原理、坐标计算、实际校准三个维度，把电阻屏的工作逻辑彻底讲透，帮你完全吃透这个技术。

一、核心结构与物理基础
1. 硬件结构
电阻屏的核心是两层透明导电薄膜（通常是 ITO，氧化铟锡）：

上层：可柔性形变的薄膜，对应Y 轴电阻层，引出两个电极 yp（Y+，接 3.3V）和 ym（Y-，接 GND）
下层：固定的硬质薄膜，对应X 轴电阻层，引出两个电极 xp（X+，接 3.3V）和 xm（X-，接 GND）
两层之间有绝缘点隔开，平时不接触；只有手指 / 触控笔按压时，两层才会在触点位置导通。

2. 核心原理：欧姆定律
电阻的本质是均匀分布的导电薄膜，相当于一根「长电阻」：

对于 X 轴层：xp到xm是一个总电阻为R_x的均匀电阻条，电压从xp(3.3V)线性降到xm(GND)
对于 Y 轴层：yp到ym是一个总电阻为R_y的均匀电阻条，电压从yp(3.3V)线性降到ym(GND)
触点位置的电压，和触点到 GND 端的距离成正比，这就是坐标计算的核心依据。


二、坐标测量的完整流程
电阻屏是分时复用的：同一时间只能测一个轴的坐标，通过切换电极的供电 / 探针状态实现。
① 测量 X 坐标（水平位置）
电极状态

X 轴层：xp接 3.3V，xm接 GND（给 X 轴层加完整电压，形成均匀电场）
Y 轴层：yp和ym悬空，仅yp作为电压探针（不供电，只读取触点电压）

原理与公式
当手指按压时，上下层在触点导通，Y 轴探针yp会读取到 X 轴层在触点位置的电压Vyp。因为 X 轴层电压是线性分布的：

触点越靠近xp（屏幕右侧），Vyp越接近 3.3V
触点越靠近xm（屏幕左侧），Vyp越接近 0V

理想均匀电阻下，坐标公式为：
X=3.3×Xmax​Vyp​​
X：触点的 X 坐标（像素值，如 0~800）
3.3：X 轴层的供电电压
Vyp：探针读取到的触点电压
Xmax：屏幕 X 方向的总电压（即 3.3V，理想情况下），本质是屏幕 X 方向的总长度对应的电压

② 测量 Y 坐标（垂直位置）
电极状态

Y 轴层：yp接 3.3V，ym接 GND（给 Y 轴层加完整电压，形成均匀电场）
X 轴层：xp和xm悬空，仅xp作为电压探针（不供电，只读取触点电压）

原理与公式
同理，手指按压导通后，X 轴探针xp读取 Y 轴层在触点位置的电压Vxp：

触点越靠近yp（屏幕下侧），Vxp越接近 3.3V
触点越靠近ym（屏幕上侧），Vxp越接近 0V


分时复用的本质
电阻屏不能同时检测 X 和 Y 坐标，必须通过切换开关（如单片机的 GPIO 配置为输出 / 输入）来分时供电和采样：
测 X 时：X 轴为输出（供电），Y 轴为输入（采样）
测 Y 时：Y 轴为输出（供电），X 轴为输入（采样）
两次采样的时间差极短（微秒级），人眼完全感知不到，看起来是「同时」响应





电容屏中有一个控制芯片，它会周期性产生驱动信号，接收电极接收到信号，
并可测量电荷大小。当电容屏被按下时，相当于引入了新的电容，从而影响了接
收电极接收到的电荷大小。主控芯片根据电荷大小即可计算出触点位置。
怎么通过电荷计算出触点位置？这由控制芯片实现，这类芯片一般是 I2C 接
口。
我们只需要编写程序，通过 I2C 读取芯片寄存器即可得到这些数据。






