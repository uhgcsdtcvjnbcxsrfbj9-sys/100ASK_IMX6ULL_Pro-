串口应用编程 = 编写程序来控制 UART（通用异步收发传输器） 接口，实现设备与设备之间（如电脑与开发板、传感器与 MCU）的串行通信。
2. 为什么叫 “应用层” 编程？
在 Linux 系统中，硬件操作分三层：
硬件层：物理串口芯片（UART）。
驱动层：内核驱动（如 serial8250），封装硬件细节。
应用层：我们写的程序，通过文件接口访问串口。
关键口诀：在 Linux 下，一切皆文件。串口在系统中表现为一个设备文件。
二、Linux 下串口的身份：设备文件
在 Linux 系统中，串口不是神秘的东西，它就是文件！
1. 常见设备名
ARM 开发板（如 IMX6ULL）：/dev/ttymxc0、/dev/ttymxc1
PC 串口（232/485）：/dev/ttyS0、/dev/ttyS1
USB 转串口（PL2303/CH340）：/dev/ttyUSB0
蓝牙串口：/dev/rfcomm0
2. 操作权限
程序操作串口，本质就是读写这个文件。
打开：open()
读取：read()
写入：write()
配置：ioctl()（控制终端接口）
三、串口通信的 5 个核心参数（必考！）
编程前必须配置串口参数，否则数据传输会乱码或失败。核心参数通过结构体 struct termios 控制。
1. 波特率（Baud Rate）
定义：传输速度（如 9600、115200、921600）。
常见值：B9600、B115200。
2. 数据位（Data Bits）
定义：每个字符包含多少位数据。
常见：8（8N1 中的 8）。
3. 校验位（Parity Bit）
定义：用于差错检测。
N (None)：无校验（最常用）
O (Odd)：奇校验
E (Even)：偶校验
4. 停止位（Stop Bits）
定义：标识一个字符传输结束。
常见：1（8N1 中的 1）。
5. 流控（Flow Control）
定义：防止数据溢出。
None：无流控（最常用）
HW (RTS/CTS)：硬件流控
SW (XON/XOFF)：软件流控


  
