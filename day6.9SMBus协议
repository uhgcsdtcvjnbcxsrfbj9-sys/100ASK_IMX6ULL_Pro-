SMBus 与 I2C 的核心关系（先分清）
很多人会把两者混为一谈，核心区别一句话总结：
SMBus 是 I2C 的「严格版、专用版」，I2C 是 SMBus 的「超集」
核心对比表（一眼看懂）
表格
特性	I2C 协议	SMBus 协议
本质	通用串行总线	基于 I2C 的系统管理专用总线
电气特性	开漏输出，上拉电阻灵活	严格规定上拉电阻范围、电平标准（3.3V/5V）
时钟频率	标准 100kHz、快速 400kHz、高速 3.4Mbps	标准 100kHz（最高仅支持 400kHz，无高速模式）
时序要求	宽松，支持不同时钟拉伸	严格的时序、超时、ACK 要求
通信格式	灵活，支持任意字节读写	固定的「地址 + 命令 + 数据」格式，强制 ACK
超时机制	无强制要求	强制超时检测（35ms），防止总线死锁
地址模式	7 位 / 10 位地址	仅支持 7 位地址
典型应用	传感器、存储、外设	电源管理、电池监控、硬件监控、EEPROM
兼容性	不兼容 SMBus 设备（时序不匹配）	兼容 I2C 主设备（SMBus 主机可驱动 I2C 从设备）
三、SMBus 的核心特性（必须记住）
1. 严格的电气与时序规范
上拉电阻：强制要求 4.7kΩ~10kΩ，电平标准统一（3.3V/5V 兼容）
时钟频率：标准模式 100kHz，快速模式最高 400kHz，不支持 I2C 的高速模式（3.4Mbps）
超时机制：强制 35ms 超时，若总线被拉低超过 35ms，主机自动复位总线，防止死锁（I2C 无此强制要求）
时钟拉伸：仅允许从设备在 ACK 阶段拉伸时钟，限制更严格
2. 固定的通信格式（区别于 I2C 的灵活）
SMBus 通信必须遵循 「地址 + 命令码 + 数据」 的固定格式，所有操作都有明确的协议定义，不像 I2C 可以自由传输字节：
写操作：Start → 设备地址(写) → ACK → 命令码 → ACK → 数据 → ACK → Stop
读操作：Start → 设备地址(写) → ACK → 命令码 → ACK → ReStart → 设备地址(读) → ACK → 数据 → ACK/NACK → Stop
命令码（Command Code）：1 字节，用来指定具体操作（如读温度、写配置），是 SMBus 区别于 I2C 的核心标识
3. 强制的 ACK 应答机制
每传输 1 字节（地址、命令、数据），必须收到 ACK，否则通信失败
最后一个字节必须发 NACK 结束，严格遵循握手流程，可靠性远高于 I2C
4. 专用的协议命令（系统管理专属）
SMBus 定义了一系列专用命令，专门用于系统管理：
Quick Command：快速命令，仅发送地址 + 读写位，无数据，用于设备检测
Send Byte：发送 1 字节数据（命令码）
Receive Byte：接收 1 字节数据
Write Byte/Word：写 1 字节 / 2 字节数据到指定寄存器
Read Byte/Word：读 1 字节 / 2 字节数据从指定寄存器
Process Call：双向读写（写命令 + 数据，读响应数据），用于复杂操作
Block Read/Write：块读写，支持多字节连续传输
