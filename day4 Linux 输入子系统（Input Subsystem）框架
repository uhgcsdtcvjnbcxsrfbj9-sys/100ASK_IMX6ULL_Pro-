一、整体分层结构（从下到上，从硬件到应用）
整个输入子系统分为 4 大层级，严格遵循「分层解耦、上下隔离」的设计思想，每一层只和相邻层交互，职责完全独立：
1. 最底层：硬件层（Hardware）
包含设备：鼠标、键盘、触摸屏、画图板、游戏手柄、按键等所有输入类硬件。
核心作用：产生原始的硬件输入信号（比如按键按下的电平变化、鼠标的位移脉冲、触摸屏的触摸坐标）。
特点：完全硬件相关，不同硬件的电气特性、通信协议（I2C/SPI/UART/USB）完全不同。
2. 第 3 层：输入系统驱动层（Drivers，硬件相关）
对应模块：drivers/input/ 目录下的各类硬件驱动（比如 usbmouse.c、atkbd.c 键盘驱动、触摸屏驱动等）。
核心职责（图中右侧说明）：
硬件数据获取：和底层硬件交互，读取原始的硬件输入数据（比如按键按下 / 抬起、鼠标 XY 轴位移、触摸屏坐标）。
标准化转换：把硬件特有的原始数据，转换成Linux 输入子系统统一的标准事件格式（struct input_event）。
向上汇报：把标准化的事件上报给上一层的「输入核心层」。
关键特点：
只负责「硬件适配 + 数据标准化」，不处理业务逻辑。
驱动开发者只需要关注硬件，不需要关心上层应用如何使用，实现了「驱动与应用解耦」。
3. 第 2 层：输入系统核心层（Input Core）
对应模块：input.c（内核核心文件，drivers/input/input.c），是整个输入子系统的「中枢神经」。
核心职责（图中右侧说明）：
承上启下：向下对接所有输入驱动，向上对接事件处理层。
事件转发：接收驱动层上报的标准输入事件，分发给上层的事件处理器（handler）。
设备管理：统一管理所有输入设备，创建设备节点（/dev/input/eventX、/dev/input/mouseX 等），提供统一的内核 API 给驱动层和事件层调用。
关键作用：
屏蔽了上层对底层硬件的感知：应用层只需要通过统一的设备节点读写，不需要关心是鼠标还是键盘驱动。
实现了「驱动与事件处理层解耦」，新增硬件驱动不需要修改上层代码。
4. 第 1 层：输入系统事件层（Input Event Handlers）
对应模块：evdev.c（最常用，对应 /dev/input/eventX）、mousedev.c（对应 /dev/input/mouseX）、kbd.c（键盘事件处理）等。
核心职责（图中右侧说明）：
事件处理：接收核心层转发的输入事件，做进一步的封装和处理。
用户空间接口：给用户空间（应用层）提供统一的访问接口，核心就是 /dev/input/event0, event1, event2... 这类设备节点。
关键特点：
不同 handler 对应不同的用户空间接口：evdev 是通用事件接口，支持所有输入事件；mousedev 是兼容旧版鼠标的接口。
应用层通过读写这些设备节点，就能获取到标准化的输入事件。
5. 最上层：用户空间（User Space）
包含模块：
直接访问的应用（APP）：比如 evtest 工具、自定义的输入测试程序，直接打开 /dev/input/eventX 读取事件。
封装库：tslib（触摸屏校准库）、libinput（Wayland/X11 桌面环境用的输入处理库），给上层 APP 提供更易用的 API，简化开发。
核心作用：
应用层通过两种方式使用输入设备：
直接访问：APP 直接打开 /dev/input/eventX，read() 读取 struct input_event 事件，适合自定义场景（比如嵌入式设备的按键处理）。
通过库访问：通过 tslib/libinput 等库，库内部已经封装了事件读取、校准、手势识别等逻辑，APP 只需要调用库函数，适合桌面、复杂 UI 场景。
