每个wchar_t占据4字节，可执行程序里wchar_t中保存的就是字符的UNICODE值


  在 Linux 下：
1 个 wchar_t 变量 = 占 4 字节内存
这 4 字节里存的，就是这个字符的 Unicode 编码值
没有任何多余转换，存的就是数字本身！

  不管是中文、英文、空格、符号，只要是 wchar_t，都占 4 个字节。
对比：
char：1 字节（存英文）
wchar_t：4 字节（存全世界所有文字）
  wchar_t 里面存的不是字符，是数字！
看到 中 → 程序里存的是 0x4E2D
看到 空格 → 程序里存的是 0x20
看到 g → 程序里存的是 0x67
这个数字，就叫 Unicode 编码。
三、为什么这 4 字节存的就是 Unicode？
因为：
L"" 前缀 告诉编译器：
把字符串转成 Unicode 编码 存起来
wchar_t 是专门存 Unicode 的类型
Linux 下的 wchar_t 就是 UTF-32 格式（固定 4 字节存一个 Unicode）




  如果 test_wchar.c 是以 ANSI(GB2312)格式保存，那么需要使用以下命令来编译：
gcc -finput-charset=GB2312 -fexec-charset=UTF-8 -o test_wchar test_wchar.c  解释


  源码文件：test_wchar.c（你写代码的文件）
文件编码：ANSI (GB2312)（Windows 常用，中文编码）
程序运行时：Linux 只认 UTF-8 / UTF-32（Unicode）
一、先讲前提
如果你的 test_wchar.c 文件，是用 ANSI (GB2312) 格式保存的
意思是：
你文件里写的 “中”，在硬盘里存的是 GB2312 编码，不是 Unicode。
但 Linux 程序运行时，wchar_t 必须是 Unicode。
所以：
编译器必须帮你做一次 “翻译”！


  -finput-charset=GB2312
告诉 GCC 编译器：
“我这个 .c 文件本身是 GB2312 编码的，你读的时候要按 GB2312 来读里面的中文！”
源码文件编码 = GB2312
告诉 gcc：别读错啦！
2. -fexec-charset=UTF-8
告诉 GCC 编译器：
“编译后，程序运行时，宽字符串 L"" 要转成 UTF-8（Unicode）存储！”
程序运行时需要 Unicode
让 gcc 自动把 GB2312 → 翻译成 Unicode


  
