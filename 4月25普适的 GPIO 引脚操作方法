先搞懂：GPIO 到底是什么？
GPIO = 通用输入输出口
你可以把它理解成单片机 / 芯片上的万能小引脚：
能输出高 / 低电平（比如控制 LED 亮灭、继电器开关）
能输入读取外部电平（比如检测按键按下 / 松开、传感器信号）

一个 GPIO 引脚要正常工作，必须经过 4 步配置：
供电 / 时钟使能：给 GPIO 模块通电，它才会工作
模式配置：选择这个引脚当 GPIO 用，而不是串口、USB 等
方向配置：设置成输出（我控制别人）或输入（我读取别人）
数值操作：
输出：写高电平 (1) / 低电平 (0)
输入：读当前是高电平 (1) / 低电平 (0)

2 GPIO 寄存器操作（核心原理）
芯片里的寄存器 = 控制 GPIO 的 “开关面板”
我们通过读写寄存器里的bit 位（0 或 1），来控制引脚。
⚠️ 最重要原则：操作某一位时，绝对不能改动其他位！


重点：GPIO 的 2 种操作方法（彻底讲清）
方法一：直接读写（Read-Modify-Write）
核心逻辑
先把整个寄存器的值读出来
只修改你想要的那一位
把修改后的值写回寄存器
1）设置某一位为 1（输出高电平）
公式：先读 → 或运算 (|) → 再写


方法二：置位 / 清零协议（Set-and-Clear Protocol）
这是芯片设计的高级功能，更简单、更安全！
芯片设计了 3 个寄存器，对应同一个物理寄存器：
data_reg：数据寄存器（正常读写）
set_reg：置位寄存器（写 1 → 对应位变 1）
clr_reg：清零寄存器（写 1 → 对应位变 0）
1）设置某一位为 1
set_reg = (1 << n);
✅ 原理：
向 set_reg 写 1，只有对应位被置 1，其他位纹丝不动
不需要读！不需要改！直接写！
2）清除某一位为 0
clr_reg = (1 << n);
✅ 原理：
向 clr_reg 写 1，只有对应位被清 0
