我们以「按下键盘按键」为例，完整走一遍数据流程，帮你彻底打通逻辑：
硬件层：键盘按下，产生硬件电平变化，通过 USB/I2C 等总线发送给驱动。
驱动层：键盘驱动读取硬件数据，识别到「A 键按下」，转换成标准事件：
// 标准输入事件结构体（所有输入设备通用）
struct input_event {
    struct timeval time;  // 事件时间戳
    __u16 type;           // 事件类型：EV_KEY（按键事件）
    __u16 code;           // 事件编码：KEY_A（A键）
    __s32 value;          // 事件值：1（按下），0（抬起）
};
然后调用 input_report_key() 等核心层 API，把事件上报给核心层。
核心层：input.c 接收事件，找到对应的事件处理器（evdev），把事件转发给它。
事件层：evdev.c 把事件放到设备节点的缓冲区，等待用户空间读取。
用户空间：
方式 1：APP 直接 open("/dev/input/event1", O_RDONLY)，read() 读取到 input_event 结构体，解析出「A 键按下」，做出响应。
方式 2：APP 调用 libinput 库，库内部帮 APP 完成设备打开、事件读取、解析，APP 只需要处理「A 键按下」的业务逻辑。
三、分层设计的核心优势（为什么要这么分？）
解耦：驱动、核心、事件、应用完全隔离，修改某一层不影响其他层。
新增一个触摸屏驱动，只需要写驱动层代码，不需要修改核心层、事件层和应用层。
应用层只需要统一的 /dev/input/eventX 接口，不需要关心底层是 USB 键盘还是 I2C 触摸屏。
复用：核心层和事件层是通用的，所有输入设备都可以复用，不需要重复开发。
可扩展：新增输入设备类型（比如手写笔、游戏手柄），只需要新增驱动层代码，上层完全兼容。
标准化：所有输入设备都用统一的 struct input_event 格式，应用层开发成本极低。
四、关键细节与应用场景（嵌入式开发必看）
1. 核心数据结构：struct input_event
这是整个输入子系统的「通用语言」，所有硬件事件都会被转换成这个结构体，应用层直接读取即可，是嵌入式开发中处理按键、触摸屏的核心。
2. 设备节点说明
/dev/input/eventX：通用事件节点，X 从 0 开始，对应每个输入设备，evtest 工具就是读取这个节点。
/dev/input/mouseX：旧版鼠标兼容节点，现在基本用 eventX 替代。
/dev/input/by-id/、/dev/input/by-path/：软链接，方便通过设备 ID / 路径找到对应节点，避免 eventX 序号变化的问题。
3. 嵌入式开发中的典型应用
按键驱动：写一个 GPIO 按键驱动，在中断中上报 EV_KEY 事件，应用层直接读取 /dev/input/eventX 处理按键。
触摸屏驱动：写 I2C/SPI 触摸屏驱动，上报 EV_ABS（绝对坐标）事件，应用层通过 tslib 校准后使用。
问题排查：当输入设备没反应时，分层排查：
硬件层：用示波器 / 万用表确认硬件信号正常。
驱动层：用 dmesg 看驱动是否加载成功，是否有报错。
核心层：用 cat /proc/bus/input/devices 查看设备是否被内核识别。
事件层：用 evtest /dev/input/eventX 测试是否能读到事件。
应用层：确认 APP 权限（需要 input 组权限或 root）、设备节点打开正确。
4. 库的作用
tslib：专门用于嵌入式触摸屏的校准、去抖、滤波库，解决触摸屏坐标不准、抖动问题，是嵌入式 Linux 触摸屏开发的标配。
libinput：桌面环境（X11/Wayland）用的输入处理库，支持手势、多点触控、设备配置，是现代 Linux 桌面的标准输入库。
五、总结
